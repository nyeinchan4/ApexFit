{{- if .Values.membershipService.enabled }}
{{- $svc := .Values.membershipService }}
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-{{ $svc.name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "apexfit.labels" . | nindent 4 }}
    app.kubernetes.io/component: {{ $svc.name }}
spec:
  type: {{ $svc.service.type }}
  selector:
    {{- include "apexfit.selectorLabels" (dict "component" $svc.name "Release" .Release) | nindent 4 }}
  ports:
    - port: {{ $svc.port }}
      targetPort: {{ $svc.port }}
      protocol: TCP
      name: http
{{- end }}
---
{{- if and .Values.membershipService.enabled .Values.membershipService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Release.Name }}-{{ .Values.membershipService.name }}
  namespace: {{ .Values.namespace.name }}
  labels:
    {{- include "apexfit.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Release.Name }}-{{ .Values.membershipService.name }}
  minReplicas: {{ .Values.membershipService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.membershipService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.membershipService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
